
  # Copy existing website project

  This is a code bundle for Copy existing website project. The original project is available at https://www.figma.com/design/J25DqoMBJ5fBepcINfkoPo/Copy-existing-website-project.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  